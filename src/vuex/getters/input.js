export const input = {
  name: state => state.input.name,
  id: state => state.input.id,
  host: state => state.input.host,
  evil: state => state.input.evil,
  users: state => state.input.users,
  isHoi: state => state.input.isHoi,
  stateAngle: state => state.input.angle,
};
